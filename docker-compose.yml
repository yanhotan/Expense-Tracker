version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Supabase credentials (frontend)
      - NEXT_PUBLIC_SUPABASE_URL=https://xvotplyriidphmpxruna.supabase.co
      - NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_P_E7KKbzmw9aL1HeW3aQpA_1TLyg4lH
      # Supabase credentials (backend - same values, different env var names)
      - SUPABASE_URL=https://xvotplyriidphmpxruna.supabase.co
      - SUPABASE_ANON_KEY=sb_publishable_P_E7KKbzmw9aL1HeW3aQpA_1TLyg4lH
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s