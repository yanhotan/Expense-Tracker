{
    "name": "expense-tracker",
    "version": "1.0.0",
    "description": "Assets for Expense Tracker app",
    "scripts": {
        "build": "echo 'Starting asset build...' && export BUNDLE_JOBS=1 && bundle config set --local without 'development test production' && bundle install --jobs=1 && echo 'Assets pre-built and ready for deployment'"
    },
    "dependencies": {},
    "devDependencies": {},
    "engines": {
        "node": ">=14.0.0"
    }
}