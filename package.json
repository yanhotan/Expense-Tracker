{
    "name": "expense-tracker",
    "version": "1.0.0",
    "description": "Assets for Expense Tracker app",
    "scripts": {
        "build": "echo 'Starting asset build...' && bundle config set --local without 'development test pg' && bundle install && bundle exec rake tailwindcss:build && bundle exec rake assets:precompile"
    },
    "dependencies": {},
    "devDependencies": {},
    "engines": {
        "node": ">=14.0.0"
    }
}