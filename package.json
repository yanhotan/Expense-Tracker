{
    "name": "expense-tracker",
    "version": "1.0.0",
    "description": "Assets for Expense Tracker app",
    "scripts": {
        "build": "export BUNDLE_PATH=vendor/bundle && bundle install --without development test && bundle exec rake assets:precompile && bundle exec rake tailwindcss:build"
    },
    "dependencies": {},
    "devDependencies": {},
    "engines": {
        "node": ">=14.0.0"
    }
}